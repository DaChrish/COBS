{$DOMAIN:localhost} {
    # API proxy (must come before file_server)
    handle /api/* {
        uri strip_prefix /api
        reverse_proxy backend:8000
    }

    # Frontend static files + SPA fallback
    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
